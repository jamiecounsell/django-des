============
Installation
============

At the command line::

    $ easy_install django-des

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-des
    $ pip install django-des
